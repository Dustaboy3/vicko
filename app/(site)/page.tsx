"use client"

import Hero from "@/components/Hero";
import RootLayout from "./layout";

const AI = () => {
  return (
    <RootLayout>
      <main style={{ height: "100vh" }}>
        <Hero />
      </main>
    </RootLayout>
  );
};

export default AI;
